To run the game first build the package using `mvn package` and then use the following command while on the top level directory:
`java -cp target/MontyHall-1.0-SNAPSHOT.jar com.amin.montyhall.MontyHall`